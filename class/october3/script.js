const toggleNav = () => {
    document.getElementById("nav-items").classList.toggle("hide-small");
};

const showToys = () => {
    let toys = ["drum", "ball", "rope", "balloon", "tire"];

    for(let i in toys) {
        
    }
}